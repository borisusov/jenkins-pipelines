pipeline {
    agent any
    tools {
        maven 'mvn_3.6.3' 
    }
    
    stages {
        stage('2 - Build') {
            steps { 
                sh "mvn clean install"
                sh "ls -la" 
                sh "pwd"
                sh "w"

            }
        }
        options {
            copyArtifactPermission('master');
            }

    }
}